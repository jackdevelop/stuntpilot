$#include "NavMesh.h"

class NavMesh
{
    static void init(int count, int arr[count]);
	static void destroy();
    static NavMesh *create();
    int findPath(int startX, int startY, int endX, int endY);
    void getPath(int count, int arr[count]);
    void printPath(void);
};

#endif
